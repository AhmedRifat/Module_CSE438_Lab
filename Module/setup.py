from setuptools import setup, find_packages

setup(name='Module',
      version='0.1',
      description='Utilities for lab exercises',
      url='https://github.com/AhmedRifat/Module_CSE438_Lab',
      packages=find_packages(),
      zip_safe=True)